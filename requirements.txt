# ============ 脚本层依赖（pip install -r requirements.txt） ============
# 本项目核心脚本依赖（全部本地/免费）
# 注意：rapidocr-onnxruntime 已锁定 1.2.3 —— 实测它在 Python 3.13 下可正常工作；
#      1.3.0~1.4.4 均要求 Python <3.13，锁定可让 3.13 用户直接装上。
pillow>=10.0.0
numpy>=1.24.0
rapidocr-onnxruntime==1.2.3
requests>=2.28.0
openpyxl>=3.1.0
# 飞书多维表格适配器依赖（可选：仅启用飞书同步时需要；克隆本地模式不需要）
lark-oapi>=1.2.0
# 测试依赖（CI smoke test 需要；克隆本地模式运行 pytest 也靠它）
pytest>=7.0.0
