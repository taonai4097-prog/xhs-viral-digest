# 生图配置指南（运营 + 开发共用）

> 适用：极光AIGC 社媒运营系统 · `pipeline/xhs_mvp.py`>   
> 更新时间：2026-09-01

---

## 一、两种模式（系统自动选，不用手动切）

| 模式            | 触发条件                      | 产出                                    | 适合谁                |
| ------------- | ------------------------- | ------------------------------------- | ------------------ |
| **提示词模式**（默认） | `.env` 里没配生图 API          | 一份《生图提示词卡片》md，运营复制提示词去豆包/即梦出图         | 没 API 额度、或嫌自动生成效果差 |
| **API 出图**    | 配了 `IMAGE_API_BASE` + Key | 直接生成 `cover.png` / `inner1~3.png` 到本地 | 有稳定 API、要批量出图      |

**降级是自动的**：即使配了 API，调用失败（401/超时）也会自动退回提示词模式，流水线不中断。  
卡片里会写明失败原因，方便排查。

---

## 二、开发者：怎么接生图 API

在 `.env` 填四行即可（支持任意 **OpenAI 兼容** 的 `/images/generations` 接口）：

```bash
IMAGE_PROVIDER=auto
IMAGE_API_KEY=sk-xxx                 # 留空则自动复用 LLM_API_KEY
IMAGE_API_BASE=https://api.openai.com/v1
IMAGE_MODEL=gpt-image-1
```

### 已验证/常用的厂商配置

| 厂商       | IMAGE_API_BASE                             | IMAGE_MODEL                      | 备注                   |
| -------- | ------------------------------------------ | -------------------------------- | -------------------- |
| OpenAI   | `https://api.openai.com/v1`                | `gpt-image-1`                    | 文字渲染能力最强，中文也稳        |
| 硅基流动     | `https://api.siliconflow.cn/v1`            | `Kwai-Kolors/kolors`             | 国内直连，有免费额度           |
| 火山方舟（豆包） | `https://ark.cn-beijing.volces.com/api/v3` | `doubao-seedream-3-0-t2i-250415` | 字节官方，和豆包同源于 Seedream |

> 模型名以各厂商控制台当前可选列表为准，填错会返回 404/400，系统会自动降级并打印原因。

### 显式覆盖（不看 .env）

```bash
python pipeline/xhs_mvp.py --inject xhs_posts/xhs_xxx.json --provider prompt_only   # 强制只出提示词
python pipeline/xhs_mvp.py --inject xhs_posts/xhs_xxx.json --provider pollinations  # 免费免key（效果一般）
python pipeline/xhs_mvp.py --inject xhs_posts/xhs_xxx.json --provider openai        # 强制走 API
```

---

## 三、运营：提示词模式怎么用（SOP）

跑完成稿后，目录里会多一个 `xhs_<选题>_生图提示词.md`，打开它：

1. **打开豆包**（或即梦）→ 点「图片」→ 粘贴卡片里的**完整提示词** → 生成
2. 一次出 4 张，**挑最贴正文、构图最工整**的那张
3. **下载不带任何文字的底图**
4. 用 **稿定设计 / Canva** 套模板把标题压上去 → 导出 **1080×1440**
5. 封面 + 3 张内页都照此办理

### 三条铁律

- **别让 AI 在图里写字**。所有生图模型渲染中文都必乱码（这是模型架构问题，不是提示词写得不好）。    
  所以系统给每张提示词都强制加了「无文字 / 无水印」，标题靠后期压。
- **别自己改提示词里的风格段**。卡片里「配色 / 风格 / 光照 / 构图 / 负向约束」那一长串是品牌锁，    
  删了就会出现「四张图四个颜色」。你只需要确认「画面主体」描述贴不贴正文。
- **同篇所有图必须同一色板**。卡片已保证，别在豆包里额外加风格词覆盖它。

### 免费商用字体（压字用，别用来源不明的）

思源黑体、思源宋体、阿里巴巴普惠体、站酷系列（庆科黄油体 / 高端黑等）。  
已有博主因使用未授权字体被索赔，务必确认授权。

---

## 四、品牌风格锁：每个账号自动分析，一套锁

**账号即一等公民**：每个号进来，`brand_analyzer.py` 自动读它自己的历史封面/文案，分析出它自己的
视觉风格 + 文案调性，生成该号专属品牌锁，落盘到 `accounts/<id>/brand.json`（视觉含 `palette/style/
lighting/composition/negative`，文案含 `voice/tone/preferred_phrases/banned_words/style_hint`）。

**一键分析某账号**：
```bash
python pipeline/brand_analyzer.py analyze \
    --account <id> \
    --cover-dir <该号历史封面目录> \
    [--corpus <该号文案语料 .txt/.md 或目录>] \
    [--label "账号展示名"]
```
- 色板用本地 K-Means(LAB) 提（纯 numpy，`seed=42` 可复现）；top-3 高分封面复制到 `references/` 作参照。
- 文案优先本地 Ollama（`qwen3-vl`，见 `.env` `BRAND_LLM_MODEL`），失败自动降级启发式 —— **不联网、不调 GLM/智谱**。
- 查看：`python pipeline/brand_analyzer.py show --account <id>`
- `_locked=true` 后 `--refresh` 整体保留旧值（人工确认版不被覆盖）；细分可用 `_visual_locked` / `_copy_locked`。

成稿时按 `--account <id>` 加载该号锁：
```bash
python pipeline/xhs_mvp.py --account <id> --inject xhs_posts/xhs_<slug>.json
```
成稿 md 头部会注入「本号文案风格锁」，运营提示词卡片也会展示 —— 保证图提示词色板、文案口吻都是
**这一号自己的**，绝不串味。缺品牌锁时脚本直接报错（退出码 2），提示先 analyze。

> 说明：早期版本在 `image_prompt.py` 里硬编码了品牌表和 `brand_kits.json` 覆盖文件；现已被
> 「按账号分析品牌锁」取代，代码不再写死任何具体账号，旧 `brand_kits.example.json` 已移除。


---

## 五、强制固定字眼（质量门禁）+ 文字模式开关

### 5.1 默认模式（allow_text=false）

每次生成的提示词**必定包含**这三个词，由 `image_prompt.py` 自动拼接，人工删不掉：

```
风格统一, 无文字, 无水印
```

### 5.2 允许文字模式（allow_text=true）

运营实测豆包 Seedream 等模型中文文字渲染尚可，可让模型**直接在画面压大标题**，省去后期压字。
在注入的 JSON **顶层加 `"allow_text": true`** 即可切换：

```json
{
  "allow_text": true,
  "topic": "...",
  "cover": {"prompt": "奶油白底信息卡，上方大号深棕色标题，三行竖向排列", ...}
}
```

该模式下后缀自动换成：

```
风格统一, 高清细节, 与同篇其他图同色板同滤镜, 无水印, 无二维码, 不堆砌小字, 标题大字清晰可读
```

同时**自动剔除品牌锁负向约束里的「无文字」**（否则提示词一边允许压标题、一边要求无文字，自相矛盾）。
⚠️ 用这个模式**务必人工复核**渲染出的中文是否清晰、有无错字漏字；一旦文字扭曲，仍退回默认无文字底图、后期压字。

### 5.3 冲突色调词自动剥离（防「四图四色」）

主体描述里若带 `dark / dramatic / high contrast / 深色背景 / 黑底 / 暗色背景 / 黑色背景 / 霓虹` 等
中英文冲突色调词，会覆盖品牌色板导致四张图四个色。系统会**自动剥离并打印告警**，卡片里也会标注。
所以主体描述请只写「内容与构图」，**不要写色调**。

对应解决你之前提的三个问题：

| 原问题      | 机制                                     |
| -------- | -------------------------------------- |
| 四张图四个颜色  | 品牌锁 style block 原样复用（配色/风格/光照/构图写死）    |
| 图片没有文字   | 强制「无文字」出底图 + 后期压字（模型渲染中文必乱码，绕不过去）      |
| 提示词风格不统一 | 模板引擎统一拼接 + `validate_prompt()` 门禁校验必含词 |

---

## 六、排查

| 现象               | 原因 / 处理                                                           |
| ---------------- | ----------------------------------------------------------------- |
| 一直只出提示词卡片，不出图    | 没配 `IMAGE_API_BASE`。这是默认行为，不是故障                                   |
| 配了 API 还是降级      | 看卡片里的失败原因：401/403 = key 或模型名错；超时 = 网络；404 = `IMAGE_API_BASE` 路径不对 |
| 出的图带乱码文字         | 模型渲染问题。检查提示词里「无文字」是否还在（别被覆盖），照旧走后期压字                              |
| 想换回免费自动出图        | `--provider pollinations`（免 key，但效果一般，已不作为默认）                     |
| 智谱 CogView 报 401 | 该 key 通常没有生图权限；本项目不默认走智谱，需显式 `--provider cogview`                 |

---

## 七、自检命令

```bash
python pipeline/image_prompt.py     # 验证提示词拼接 + 必含词门禁
python pipeline/image_provider.py   # 验证 provider 降级链 + 尺寸推导
```
